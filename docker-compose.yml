version: '3.8'

services:
  db:
    image: postgres:15
    container_name: postgres-estoque
    restart: always
    environment:
      POSTGRES_DB: estoquedb
      POSTGRES_USER: admin
      POSTGRES_PASSWORD: 123
    ports:
      - "5432:5432"